#include <stdio.h>
int main()
{
    int n, a=1;
    printf("Enter number of rows :");
    scanf("%d", &n);
    for (int i = 1; i <= n; i++){
        for (int j = i; j <= n; j++)
        {
            printf(" ");
        }
    int d = a+64
    ;
    for (int j = 1; j <= n; j++){
        char ch = (char)d;
    printf("%c ", n);
    a++;
    }
    printf("\n");
    }
}