#include <stdio.h>
#include <string.h>
int main()
{

    char *s1 = "Hello";
    char *s2 = "Duniya";
    printf("%s", strcat(s1, s2));

    // int x = strlen(str);
    // char s1[x];
    // strcpy(s1,str);
    // s1[0]='p';
    // printf("%s",s1);
}